<% title("#{@post.title}_购物指南") %>
<% path(@path) %>
<% json_ld(@json_ld) %>

<div id="detail-page">
  <div class="item-wrap">
    <header class="article-header" data-pos="1">
      <div class="article-title">
        <h1><%= @post.title %></h1>
      </div>
    </header>
  </div>
  <div class="item-wrap">
    <article>
      <div class="article-body">
        <p><%= @post.description %></p>
        <p>下面是给大家推荐的潮货好物：</p>
        <% @products.each_with_index do |product, index| %>
          <div class="mip-img-container">
            <mip-img src="http:<%= product.pic_url %>"></mip-img>
          </div>
          <p>
            NO.<%= index + 1 %>&nbsp;<%= product.title %>
            <br/>
            <% if product.price > 0 %>
              促销价：<%= product.price %>元  原价：<%= product.origin_price%>元  
            <% else%>
              价格：<%= product.origin_price%>元
            <% end %>
            <br/>
            <a href="http://www.shikuaigou.com/buy/<%= product.item_id%>/?activity_id=" target="_blank" class="get-coupon">
              <<点我购买>>
            </a>
          </p>
        <% end %>
        <p> - THE <%= @post.title%> END -</p>
      </div>
      <footer class="article-footer clear"></footer>
    </article>
  </div>
  <% if !@related_posts.nil? && @related_posts.size > 0  %>
    <div class="item-wrap">
      <section class="related-news section" data-pos="2">
        <div class="section-title">
          <h2>相关推荐</h2>
        </div>
        <ul>
          <% @related_posts.each do |post| %>
            <li><a href="/gpost/<%= post.id %>/"><%= post.title %></a></li>
          <% end %>
        </ul>
      </section> 
    </div>
  <% end %>
  <div class="item-wrap">
    <section class="article-notice">
      <p>更多潮货搭配，请关注 —— 购物指南</p>
    </section>
  </div>
</div>

